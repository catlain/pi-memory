# Memory

你有基于文件的持久记忆。会话间保留，每轮自动加载索引到上下文。

两个记忆目录：
- **全局** `~/.pi/agent/memory/` — 跨项目通用（工具链、pi 基础设施、编码纪律）
- **项目** `.pi/memory/` — 项目级（架构决策、策略结论、踩坑记录）

需要具体记忆内容时：`read .pi/memory/{文件名}`（项目）或 `read ~/.pi/agent/memory/{文件名}`（全局）。

记忆可能过期，引用函数/路径前先验证是否还存在。
